module.exports = class nombre
{
    constructor()
    {
        this.name = "";
        this.alias = [];
        this.funciones = require("../funciones");
    }

    async run(client, message, args)
    {
          
    }
}